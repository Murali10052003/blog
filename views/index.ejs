<div class="author">
    <h1 class ="author__heading">Hi, I am Murali</h1>
    <p class="author__body">Web developer</p>
</div>

<img src="/img/hero-image.webp" alt="onnu illa" class="hero-image" width="982" height="528">


<section class="articles">
    <h2 class="articles__heading">Latest posts</h2>

    <ul class="article-ul">

             <% data.forEach(post => { %>
                <li><a href = "/post/<%= post._id %>"
                    >
                        <span><%= post.title %></span>
                        <span class="article-list__data"><%= post.createdAt.toDateString() %></span>
                    </a>
                </li>
             
              
             <% }) %>   
            
     
        
    </ul>

    <% if (nextPage !== null) { %>
        <a href="/?page=<%= nextPage %>" class="pagination">&lt; view older Posts</a>
    <% } %>
    
</section>